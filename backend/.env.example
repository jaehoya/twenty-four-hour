PORT=4000

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=tfh_db
DB_USER=root
DB_PASSWORD=your_password

# Security
BCRYPT_SALT_ROUNDS=10
JWT_SECRET=your_secret_key
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Email
EMAIL_USER=your_email
EMAIL_PASS=your_email_password

# AI
GEMINI_API_KEY=your_gemini_api_key

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# Upload
UPLOADS_ROOT=/home/ubuntu/uploads

# Swagger (local / server configurable)
SWAGGER_HOST=localhost:4000
SWAGGER_SCHEME=http
SWAGGER_SERVER_URL=http://localhost:4000

# Frontend (CORS / redirect 용)
FRONTEND_BASE_URL=http://localhost:5173
